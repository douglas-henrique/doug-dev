import { blogPosts } from '@/lib/content'
import { desc } from '@/lib/descriptions'

export const metadata = {
  title: 'Blog',
  description: desc('en/blog'),
  alternates: {
    canonical: '/en/blog',
    languages: {
      en: '/en/blog',
      pt: '/pt/blog',
      'x-default': '/en/blog'
    }
  }
};

# Blog

<PostList showAuthor posts={blogPosts.en} />
